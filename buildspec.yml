phases:
  install:
    runtime-versions:
      python3
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - python test.py
  post_build:
    commands:
      - echo Build completed on `date`
